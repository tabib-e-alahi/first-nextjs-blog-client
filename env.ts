import { createEnv } from "@t3-oss/env-nextjs"; // or core package
import { z } from "zod";

export const env = createEnv({
      server:{
            BACKEND_API=
            FRONTEND_API=

            API_URL=
            AUTH_URL=
      },
      runtimeEnv
})